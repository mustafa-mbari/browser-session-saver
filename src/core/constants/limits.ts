/** Maximum allowed size (bytes) for an imported session JSON file (5 MB). */
export const MAX_IMPORT_SIZE_BYTES = 5_242_880;

/** Percentage of quota usage above which a warning indicator is shown. */
export const QUOTA_WARNING_PCT = 80;
